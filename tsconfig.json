{
  "extends": "./packages/typescript-config/tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true, // 各アプリでビルドを行うため、ルートでは出力を無効に
    "baseUrl": ".", // モノレポのルートを基準に
  },
  "exclude": ["node_modules", "dist", "apps/*/dist", "packages/*/dist"]
}
